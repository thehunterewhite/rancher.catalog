---
version: "2"
services:
  heimdall:
    image: linuxserver/heimdall
    container_name: heimdall

    ports:
      - ${http_port}:80

    environment:
      - PUID=1000
      - PGID=1000
      - TZ="America/New_York"

    restart: unless-stopped
